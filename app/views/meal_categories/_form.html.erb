<%= form_for(@meal_category) do |f| %>
  <% if @meal_category.errors.any? %>
    <div id="error_explanation">
      <h5>
        Oops!<%= pluralize(@meal_category.errors.count, "error") %> prohibited this meal_category from being saved:</h5>

      <ul>
      <% @meal_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
