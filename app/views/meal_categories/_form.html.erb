<%= form_for(@meal_category) do |f| %>
  <% if @meal_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal_category.errors.count, "error") %> prohibited this meal_category from being saved:</h2>

      <ul>
      <% @meal_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
